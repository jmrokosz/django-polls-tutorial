version: 1.0
runtime: python3
build:
  commands:
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt
run:
  runtime-version: 3.11
  command: gunicorn djangotutorial.wsgi:application --bind 0.0.0.0:8000
network:
  port: 8000
